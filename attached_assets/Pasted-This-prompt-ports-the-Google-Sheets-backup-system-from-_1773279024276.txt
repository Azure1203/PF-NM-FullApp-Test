This prompt ports the Google Sheets backup system from the production app. It is a straightforward port — copy the logic exactly. There are two parts: a manual backup triggered from the dashboard, and a daily automated backup at 3:00 AM.

Part 1 — server/googleSheets.ts
Verify this file exists and exports getGoogleSheetsClient() and getGoogleDriveClient(). Both use Replit Connectors OAuth — the same token-fetch pattern used by the Outlook integration. If the file is missing, create it by copying from the production app exactly.

Part 2 — server/backupScheduler.ts
Create this file by porting from the production app exactly.
Constants:
typescriptconst FOLDER_NAME = 'Perfect Fit Orders Replit Backup';
Module-level state:
typescriptlet schedulerTimeout: NodeJS.Timeout | null = null;
let lastBackupTime: string | null = null;
let lastBackupStatus: 'success' | 'error' | null = null;
let lastBackupError: string | null = null;
Scheduling logic:

getNextRunTime() — returns a Date for the next 3:00 AM (tomorrow if 3 AM has already passed today)
getMsUntilNext() — milliseconds until next run
scheduleNext() — sets a setTimeout for the next run, then calls itself recursively after each run
startBackupScheduler() — logs startup message and calls scheduleNext()
stopBackupScheduler() — clears the timeout
getBackupSchedulerStatus() — returns { nextRun, lastBackupTime, lastBackupStatus, lastBackupError }

runScheduledBackup() — the core export function, also called manually:

Get sheets and drive clients
Create a new Google Spreadsheet with title PF Order Backup - {ISO timestamp} and 6 sheets: Orders, Order Files, Products, Pallets, Hardware Checklist, Packing Checklist
Find or create the Drive folder named Perfect Fit Orders Replit Backup
Move the spreadsheet into that folder (get current parents, remove them, add new folder)
Query all data from the database:

projects table → Orders tab
orderFiles table → Order Files tab
products table → Products tab
pallets table → Pallets tab
hardwareChecklistItems table → Hardware Checklist tab
packingSlipItems table → Packing Checklist tab


Build each sheet's data as a 2D array with a header row, then write using sheets.spreadsheets.values.batchUpdate
Apply bold blue header formatting using sheets.spreadsheets.batchUpdate with repeatCell requests

Exact column layouts (copy these exactly):
Orders tab header:
ID, Name, Date, Dealer, Shipping Address, Phone, Tax ID, Order ID, Status, Asana Task ID, Asana Section, PF Order Status, PF Production Status, Cienapps Job #, Notes, Created At
For pfProductionStatus: join the array with ', ' since it's a jsonb array in the DB.
Order Files tab header:
ID, Project ID, Filename, PO Number, Core Parts, Dovetails, Assembled Drawers, 5-Piece Doors, Weight (lbs), Max Length, Max Width, Has Glass, Glass Inserts, Glass Shelves, Has MJ Doors, Has Richelieu Doors, Has Double Thick, Has Shaker Doors, MJ Doors Count, Richelieu Doors Count, Double Thick Count, Wall Rail Pieces, Allmoxy Job #, HW BO Status, Packaging Link, Notes, Created At
For boolean fields: write 'YES' or 'NO'.
Products tab header:
ID, SKU, Name, Supplier, Category, In Stock, Is Buyout, Weight, Notes, Created At
Pallets tab header:
ID, Project ID, Pallet Number, Size, Notes, Created At
Hardware Checklist tab header:
ID, File ID, Product SKU, Product Name, Quantity, Is Buyout, Buyout Option, Packed, Checked By, Checked At, Sort Order, Created At
Packing Checklist tab header:
ID, File ID, Item Code, Description, Color, Quantity, Width, Height, Depth, Cut Length, Is CTS, Checked, Checked By, Checked At, Sort Order, Created At
Header formatting — apply to row 0 of every sheet:
typescriptbackgroundColor: { red: 0.2, green: 0.4, blue: 0.7 }
textFormat: { bold: true, foregroundColor: { red: 1, green: 1, blue: 1 } }
On success: set lastBackupTime, lastBackupStatus = 'success', lastBackupError = null, log total records exported.
On error: set lastBackupTime, lastBackupStatus = 'error', lastBackupError = err.message, log the error message.
Return the spreadsheet URL on success so the manual trigger can open it.

Part 3 — Start the scheduler from server/index.ts
typescriptimport { startBackupScheduler } from './backupScheduler';

try {
  startBackupScheduler();
} catch (e: any) {
  console.error('[Backup] Failed to start scheduler:', e.message);
}
```

---

### Part 4 — API routes in `server/routes.ts`

Add or verify:
```
POST /api/backup/google-sheets
```
- Requires `isAuthenticated`
- Calls `runScheduledBackup()` (or the equivalent inline logic, same as the scheduler but returns the URL)
- On success: returns `{ success: true, url: spreadsheetUrl, title }`
- On error: returns `{ success: false, error: err.message }`
```
GET /api/backup/status

Requires isAuthenticated
Returns getBackupSchedulerStatus()


Part 5 — Frontend: Backup button on dashboard
In client/src/pages/daily/OrderProcessingDashboard.tsx, add a "Backup" button in the dashboard header area near the other utility buttons (alongside the email sync controls added in Prompt 9).
Behaviour:

Calls POST /api/backup/google-sheets
Shows a spinner while running (backup can take 10–30 seconds)
On success: shows a toast "Backup complete" and opens the returned url in a new tab with window.open(url, '_blank')
On error: shows an error toast with the message
Button label: "Backup to Sheets" with a Google Sheets–style icon (use Sheet or Table from lucide-react, or a generic database icon)

Also add a small status line below the button showing the next scheduled backup time from GET /api/backup/status, formatted as "Next auto-backup: 3:00 AM".

After completing, confirm:

App starts with the backup scheduler log line appearing
POST /api/backup/google-sheets creates a real spreadsheet in Google Drive under the Perfect Fit Orders Replit Backup folder
The dashboard shows the Backup to Sheets button and opens the new spreadsheet on success