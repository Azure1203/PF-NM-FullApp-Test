Prompt A — Fix Export Template Placeholder Resolution
Paste this entire prompt into Replit.
Problem: The generateOrdItemBlock function in server/services/ordExporter.ts builds a scope where export template placeholders like {{product_name}}, {{width}}, {{height}}, {{thickness}}, {{quantity}}, and {{edge_left.export}} cannot resolve. The result is that all ORD export blocks have empty values where dimensions, SKU names, and edge banding codes should appear.
Root cause: The function puts raw item data under scope.item (nested) but templates expect top-level {{width}} not {{item.width}}. Edge banding is computed as a single concatenated string but templates expect individual {{edge_left.export}} objects. And product_name is never injected into the scope at all.
This prompt fixes one file: server/services/ordExporter.ts. No other files need changes.

Replace the entire generateOrdItemBlock function
In server/services/ordExporter.ts, find the existing generateOrdItemBlock function (everything from its JSDoc comment through its closing brace). Replace it entirely with:
ts/**
 * Generates an .ORD item block (or any export template) by replacing {{path.to.key}} placeholders.
 * 
 * Scope resolution order:
 * 1. Top-level item properties: product_name, width, height, length, thickness, quantity
 * 2. Edge banding objects: edge_left.export, edge_right.export, edge_top.export, edge_bottom.export
 * 3. Context scope (grid alias namespaces): color.panel_export, shelves.base_price, etc.
 * 4. Computed helpers: edgebanding (concatenated EENN string)
 */
export function generateOrdItemBlock(
  itemData: any,
  contextScope: any,
  templateString: string,
  itemNumber: number = 1
): string {
  // Resolve the SKU / product name from the raw CSV item.
  // CSV column is "Manuf code" but the spread pricingItem may also carry it as other variants.
  const productName: string = (
    itemData['Manuf code'] || itemData.MANU_CODE || itemData.SKU ||
    itemData.product_name || itemData.sku || ''
  ).toString().trim();

  // Compute individual edge banding values as E/N
  const edgeLeft   = (itemData.Left   === '1' || itemData.Left   === 1) ? 'E' : 'N';
  const edgeRight  = (itemData.Right  === '1' || itemData.Right  === 1) ? 'E' : 'N';
  const edgeTop    = (itemData.Top    === '1' || itemData.Top    === 1) ? 'E' : 'N';
  const edgeBottom = (itemData.Bottom === '1' || itemData.Bottom === 1) ? 'E' : 'N';

  // Build a flat scope with all values templates might reference.
  // Top-level keys match the {{placeholder}} names used in export proxy formulas.
  const scope: Record<string, any> = {
    // --- Item dimensions (top-level, matching template placeholders) ---
    product_name: productName,
    width:        Number(itemData.width    || itemData.Width    || itemData['Width(R)'] || 0),
    height:       Number(itemData.height   || itemData.Height   || 0),
    length:       Number(itemData.length   || itemData.Length   || itemData['Length(L)'] || itemData.depth || 0),
    thickness:    Number(itemData.thickness || itemData.Thickness || 0),
    quantity:     Number(itemData.quantity  || itemData.Quantity || itemData.Qty || 1),

    // --- Edge banding as individual objects with .export property ---
    // Templates use: {{edge_left.export}}{{edge_right.export}}{{edge_top.export}}{{edge_bottom.export}}
    edge_left:   { export: edgeLeft },
    edge_right:  { export: edgeRight },
    edge_top:    { export: edgeTop },
    edge_bottom: { export: edgeBottom },

    // --- Concatenated banding string (for templates that use {{edgebanding}}) ---
    edgebanding: `${edgeLeft}${edgeRight}${edgeTop}${edgeBottom}`,

    // --- Grid context namespaces (color.panel_export, shelves.base_price, etc.) ---
    ...contextScope,

    // --- Raw item data accessible via {{item.COLUMN_NAME}} if needed ---
    item: itemData,

    // --- Additional aliases some templates may use ---
    this_part: itemData,
  };

  // Replace all {{path.to.key}} placeholders by walking the scope object
  return templateString.replace(/\{\{([\w.]+)\}\}/g, (match, path) => {
    const parts = path.split('.');
    let current: any = scope;
    for (const part of parts) {
      if (current != null && typeof current === 'object' && part in current) {
        current = current[part];
      } else {
        // Placeholder not found — return empty string
        return '';
      }
    }
    return String(current ?? '');
  });
}

Also update the formatEdgebanding function
The existing formatEdgebanding function is still used by other code paths. Keep it, but make sure it handles the same column name variants. Find:
tsexport function formatEdgebanding(item: any): string {
  const left = item.Left === '1' || item.Left === 1 ? 'E' : 'N';
  const right = item.Right === '1' || item.Right === 1 ? 'E' : 'N';
  const top = item.Top === '1' || item.Top === 1 ? 'E' : 'N';
  const bottom = item.Bottom === '1' || item.Bottom === 1 ? 'E' : 'N';
  return `${left}${right}${top}${bottom}`;
}
Replace with:
ts/**
 * Formats edgebanding from 1/0 columns to Cabinet Vision 'E'/'N' notation.
 * Handles both title-case (Left) and lowercase (left) column names.
 */
export function formatEdgebanding(item: any): string {
  const left   = (item.Left   ?? item.left)   === '1' || (item.Left   ?? item.left)   === 1 ? 'E' : 'N';
  const right  = (item.Right  ?? item.right)  === '1' || (item.Right  ?? item.right)  === 1 ? 'E' : 'N';
  const top    = (item.Top    ?? item.top)     === '1' || (item.Top    ?? item.top)    === 1 ? 'E' : 'N';
  const bottom = (item.Bottom ?? item.bottom) === '1' || (item.Bottom ?? item.bottom) === 1 ? 'E' : 'N';
  return `${left}${right}${top}${bottom}`;
}

What NOT to change

Do not modify generateOrdHeader — it works correctly
Do not modify the pricing engine (pricingEngine.ts)
Do not modify the upload pipeline in routes.ts
Do not modify the formula tester routes
Do not modify any frontend files


After completing, confirm:

The app starts without errors
Open the Formula Tester page
Pick a shelf product (e.g. 34SHFF) that has a shelves_export proxy assigned
Enter test values: Width = 355.625, Height = 685.8125, choose a Color value that resolves the color grid
The Export Text section should now show a complete ORD block with:

Materials="..." populated from color.panel_export (not empty)
Attribute="Banding","xPFC_BAND","text","EENN" (or similar — not empty "")
1,"34SHFF",355.625,685.8125,19,"*","N",1 (actual dimensions and SKU — not empty)


Pick a dovetail drawer box product (e.g. DBX36_18) with elias_export assigned
Enter dimensions with Length value
Export text should show the Elias CSV row with actual dimensions and elias_* fields populated
Pick a hardware product with hardware_export assigned
Export text should show the simple {{quantity}},{{product_name}},,,,HARDWARE format with actual values
Re-upload a test order CSV — the generated .ORD file should now contain populated item blocks instead of empty placeholders